export enum InputType {
  EMAIL = 'email',
  PASSWORD = 'password',
  TEXT = 'text',
}

export enum InputSize {
  DEFAULT = 'default',
  SMALL = 'small',
}

export enum InputColor {
  GRAY = 'gray',
  DEFAULT = 'default',
}
